import { get } from 'lodash';
// import config from 'react-native-config';
// import config from '../app/config';

// export function getConfigString(name: string, defaultValue?: string): string | undefined {
//   return get(config, name, defaultValue);
// }

// export function getFeatureFlag(name: string): boolean {
//   return get(config, name) === 'ON';
// }
//
// export function getApiURL() {
//   const keyName = 'API_URL';
//   return getConfigString(keyName);
// }
